# Build outputs
build/

# Local Claude Code settings (per-user, not shared)
.claude/settings.local.json

# OS / editor cruft
Thumbs.db
.DS_Store

# ADC quality test captured data (per-run, not source)
adc_quality_test/data/
__pycache__/
